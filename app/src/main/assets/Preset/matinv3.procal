/**
 * Matrix 3x3 Inversion
 * Returns 3x3 inverted matrix. Enter the numbers in the matrix in rows from left to right, then top to bottom.
 * @param a (1,1)
 * @param b (1,2)
 * @param c (1,3)
 * @param d (2,1)
 * @param e (2,2)
 * @param f (2,3)
 * @param g (3,1)
 * @param h (3,2)
 * @param i (3,3)
 * @return Inverted matrix
 * @sampleIn 1, 2, 3, 4, 5, 6, 7, 8, 1
 * @sampleOut -1.7917, 0.9167, -0.125, 1.5833, -0.8333, 0.25, -0.125, 0.25, -0.125
 */
?->$a: ?->$b: ?->$c:
?->$d: ?->$e: ?->$f:
?->$g: ?->$h: ?->$i:
$a$e$i + $b$f$g + $c$d$h - $c$e$g - $b$d$i - $a$f$h -> $det:
($e$i - $f$h) / $det display
($c$h - $b$i) / $det display
($b$f - $c$e) / $det display
($f$g - $d$i) / $det display
($a$i - $c$g) / $det display
($c$d - $a$f) / $det display
($d$h - $e$g) / $det display
($b$g - $a$h) / $det display
($a$e - $b$d) / $det display
