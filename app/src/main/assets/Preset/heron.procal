/**
 * Heron's Formula
 * Given the length of three sides of a triangle, returns the area of triangle.
 * @param Length of three sides of a triangle
 * @return Area of triangle
 * @sampleIn 3, 4, 5
 * @sampleOut 6
 */
?->$a: ?->$b: ?->$c:
($a+$b+$c)/2 -> $s:
sqrt($s*($s-$a)*($s-$b)*($s-$c)) 
