/**
 * Matrix 2x2 Inversion
 * Returns 2x2 inverted matrix
 * @param Matrix entered from left to right, downwards
 * @return Inverted matrix
 * @sampleIn 3, 5, 1, 2
 * @sampleOut 2, -5, -1, 3
 */
?->$a: ?->$b: 
?->$c: ?->$d:

$a$d - $b$c -> $det:
$d/$det display
-$b/$det display
-$c/$det display
$a/$det display
